spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=MYSQL;"
    username: sa
    password: sa
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop

  liquibase:
    enabled: false
